<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
    </head>
    <body>
        <form name="myForm" action="/exam_postForm" method="post">
            <label for="name">이름</label>
            <input type="text" id="name" name="name" required />

            <fieldset style="width: 800px">
                <legend>성별</legend>
                <input id="men" type="radio" name="gender" value="남자" required />
                <label for="men">남자</label>
                <input id="women" type="radio" name="gender" value="여자" required />
                <label for="women">여자</label>
            </fieldset>

            <fieldset style="width: 800px">
                <legend>생년월일</legend>
                <select>
                    <optgroup id="year" name="year">
                        <% for(let y = 2000; y <= new Date().getFullYear(); y++) { %>
                        <option value="<%=y %>"><%=y %></option>
                        <% } %>
                    </optgroup>
                </select>
                <label for="year">년</label>

                <select id="month">
                    <optgroup name="month">
                        <% for(let i = 1; i <=12; i++) { %>
                        <option value="<%= i %>"><%= i %></option>
                        <% } %>
                    </optgroup>
                </select>
                <label for="month">년</label>

                <select id="day">
                    <optgroup name="day">
                        <% for(let j = 1; j <=31; j++) { %>
                        <option value="<%= j %>"><%= j %></option>
                        <% } %>
                    </optgroup>
                </select>
                <label for="day">년</label>
            </fieldset>
            <fieldset style="width: 800px">
                <legend>선택</legend>
                좋아하는 색상<input type="color" name="color" /> <br />
                좋아하는 숫자<input type="number" name="num" />
            </fieldset>
            <button type="submit">회원가입</button>
        </form>

        <hr />
        <hr />
        <form name="myForm" action="/exam_getForm" method="get">
            <label for="name">이름</label>
            <input type="text" id="name" name="name" required />

            <fieldset style="width: 800px">
                <legend>성별</legend>
                <input type="radio" name="gender" value="남자" required />남자
                <input type="radio" name="gender" value="여자" required />여자
            </fieldset>

            <fieldset style="width: 800px">
                <legend>생년월일</legend>
                <select name="year">
                    <optgroup name="year">
                        <% for(let y = 2000; y <= new Date().getFullYear(); y++) { %>
                        <option value="<%=y %>"><%=y %></option>
                        <% } %>
                    </optgroup></select
                >년
                <select name="month">
                    <optgroup name="month">
                        <% for(let i = 1; i <=12; i++) { %>
                        <option value="<%= i %>"><%= i %></option>
                        <% } %>
                    </optgroup></select
                >월
                <select name="day">
                    <optgroup name="day">
                        <% for(let j = 1; j <=31; j++) { %>
                        <option value="<%= j %>"><%= j %></option>
                        <% } %>
                    </optgroup></select
                >월
            </fieldset>
            <fieldset style="width: 800px">
                <legend>관심사</legend>
                <input type="checkbox" name="interest" value="여행" />여행
                <input type="checkbox" name="interest" value="패션" />패션
                <input type="checkbox" name="interest" value="음식" />음식
            </fieldset>
            <button type="submit">회원가입</button>
        </form>
    </body>
</html>
